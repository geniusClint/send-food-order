<div class="min-h-full flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div>
      <%= link_to root_path do%>
        <%= image_tag("sfo_logo_2.svg", class: "mx-auto h-48 w-auto", :alt => "Send Food Order Logo") %>
      <% end %>
      <h2 class="mt-6 text-3xl font-brand text-accent-400">
        Forgot Password
      </h2>
    </div>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="pb-4">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email" %>
      </div>
      <div class="actions">
        <%= f.submit "Send reset instructions" %>
      </div>
    <% end %>
    <div class="p-3">
      <%= link_to "Don't have an account? Sign up", new_user_registration_path %><br />
      <br/>
      <%= link_to "Already Registered? Log in", new_user_session_path %><br />
      <%= link_to "Resend confirmation email", new_user_confirmation_path %>
    </div>
  </div>
</div>